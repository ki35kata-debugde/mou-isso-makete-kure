# 修正表（バグ・仕様変更の受付リスト）

> 運用ルール：
> 1. バグ・仕様変更はまずこの表に**貯めるだけ**（すぐ修正しない）
> 2. 「修正プランを立ててください」の指示で Claude がプランを作成
> 3. プラン承認後、実行修正は ChatGPT (GPT-5.6 sol) に依頼

| # | 日付 | 種別 | 内容 | 該当箇所（分かれば） | 状態 |
|---|------|------|------|----------------------|------|
| 1 | 2026-07-15 | バグ | 輸送時に在庫があるのに「現庫0」と表示されることがある（例：剣3→表示0、＋押下で表示2に減る） | mou_isso_v0_6_1.html：現庫表示の更新経路（changePrepQty L1933 / updateTransportUI L2154 / ensureOrderUI L9479、stockOf 二重定義 L3051・L6228） | 完了 |
| 2 | 2026-07-15 | バグ | T2以降、市場の前期比が全品目±0になる。**原因特定済み**：ターン送り時にコアgoToNextTurn内のupdateInvDisplay→refreshMarketUIが先に新季節価格へ再計算した後、L5779のrecomputePrices(true)がprevBuyを新価格で上書きするため。最小修正案＝L5779をrecomputePrices(false)に（prevBuy保存はL8481/L6892の事前スナップショットに任せる）。データ（seasonPriceDelta）は正常でHTML埋め込みとも一致確認済み | mou_isso_v0_6_1.html：L5779（recomputePrices(true)）／L2028（コアのupdateInvDisplay）／L5805・L8481・L6892のラッパー群 | 完了 |
| 3 | 2026-07-15 | 仕様変更 | 進言費用を「信頼度（褒める）＝300両」以外はすべて150両に | mou_isso_v0_6_1.html：adviceCost()定義、kgPickラッパーL6409、費用表示L6404・L10806 | 完了 |
| 4 | 2026-07-15 | バグ | ノード2にいる武器の皇子・兵器の皇子へ輸送するとき、品目行（アイコン・現庫・依頼残・輸送中の行）の表示が小さく・暗くなる（スクリーンショットあり: スクリーンショット 2026-07-14 204111.png。剣の行が灰色がかって文字が薄い） | 原因特定：新レンダラの `.tp-item-line` は暗い輸送パネル上で背景なし・12px、補足文字は暗色 `--faded` のため、ロック行のように小さく暗く見えていた。動的な locked クラス付与やアイコン注入親styleはなし。品目行限定で明るい panel 背景・通常文字色・opacity:1・13pxを指定 | 完了 |
| 5 | 2026-07-15 | バグ | 馬の王子が出立後、同じターン内にすぐ納品（馬の選択画面）ができてしまう。仕様：出立を押したターンは何もできず、納品は次ターン以降にすること | `goTransportHorsePrince()` の既存ガードを確認し、`buildHorsePrinceSendUI()` にも sortieTurn/turn ガードを追加。同ターンは馬チェック欄を案内文へ置換し、既存Goボタンも除去 | 完了 |
| 6 | 2026-07-15 | 仕様変更 | 輸送手段「荷車」の表記と仕様：現在「荷車(空き50/50・積載2)」→ 正しくは「荷車(要20金・積載2)」。使用時に金20両かかるが積載2で運べる仕様にする。また荷車の解禁時に通知がなかったので、結果フェーズ等でOKボタン付きポップアップで「お金（20両）がかかりますが容量2で運べます」の内容を告知する | 荷車ラベルを「要20金・積載2」に統一。最終の貨物・台数検証後かつ在庫消費前に20両不足ガードと課金を追加。既存 `pendingUnlocks` のOK付きモーダルを利用し、解禁本文に費用と積載量を明記 | 完了 |
| 7 | 2026-07-15 | バグ | #3修正の残件：「王子を褒める」の進言も150両で課金されてしまう（正しくは300両）。その他の進言が150になったのは正しい | `kgPick` 全再代入を確認。課金する2箇所を `window.adviceCostFor(val)` 優先へ統一し、最終ラッパーがクリック時の `val` を保持することも確認。`commend` は300、その他は150を直接判定し、保持値は `finally` で確実にクリア | 完了 |
| 8 | 2026-07-15 | バグ | 進言「市場を強化」を選択しても効果が見えない | NodeのDOM非依存ロジック調査で、突破直後・数ターン後とも `marketLv:0→1`、`marketLvMax:1`、店在庫上限`20→28`を確認。タイミング差は再現せず、ブラウザ実機固有の原因は未特定のため推測修正は行わない。**【修正プラン】** ロジックは正常に動いているが「上限」自体が画面上どこにも表示されない（店頭は在庫数のみ表示、`id="shop-med"`等）ため、marketLv上昇後も上限は季節ごとの入荷処理（`applySeasonStockDelta`/`seasonRestock`, L8449-8489）でのみ使われ、即時の見た目変化が無いのが「効果が見えない」の実体と推定。①店頭在庫表示を「在庫/上限」形式（例：`12/28`）に変更し、`gs.market.shopCap[k]`を隣に出す。②`applyPendingAdvice`内`market_up`分岐（L5760-5764）のトーストに加え、#9で実装済みの今季イベントログ（金色）にも「市場強化：店在庫上限 20→28」のように具体的数値変化を出す。表示追加のみで計算ロジックは変更しないため低リスク | プラン済 |
| 9 | 2026-07-15 | 仕様変更 | 今季イベントログに進軍・解禁・進言解放を色分け表示 | Sonnetにより直接再実装（末尾追記スクリプト）。`pushTurnEvent`/`gs.turnEvents`を新設し、ターン送り開始時にリセット。進軍（黄）・荷車/船/馬車解禁（赤）・進言解放（赤）・進言効果（金、#13統合）を`result-log`に描画。静的検証（JSON整合性・script構文）は通過。**要実機確認** | 実装済み（要確認） |
| 10 | 2026-07-15 | 仕様変更 | 機能していない解禁モーダルを削除 | Sonnetにより実装：`showPendingUnlockModal`を無効化（no-op化）し、既存の`#unlock-modal-overlay`要素があれば削除。解禁通知は#9の今季ログに一本化 | 実装済み（要確認） |
| 11 | 2026-07-15 | 仕様変更 | 結果フェーズ生産画面から完成品在庫行を削除。今季生産したものを表示 | Sonnetにより実装：前回の原因（`goToResult()`が`buildResultScreen()`より先に`gs.inProd`をゼロ化していたため「生産中」欄が常に空だった）を修正。`goToResult()`をラップし、ゼロ化前に`gs.prodThisTurn`へスナップショットを取り、結果画面はそのスナップショットを表示するよう変更 | 実装済み（要確認） |
| 12 | 2026-07-15 | バグ | 過去の手渡し済みが毎季表示される | Sonnetにより再実装：`handDeliver`/`handDeliverHorse`をラップして完了時に`gs.prepDoneTurn[key]=gs.turn`を記録し、結果画面側で`gs.turn`と一致する場合のみ表示 | 実装済み（要確認） |
| 13 | 2026-07-15 | 仕様変更 | 結果フェーズ「謁見の結果」欄を削除 | Sonnetにより再実装：`result-kengen`のセクションを非表示化。進言効果メッセージは`applyPendingAdvice`をラップして#9の今季ログ（金色）へ集約済みなので情報は失われない | 実装済み（要確認） |
| 14 | 2026-07-15 | 仕様変更 | 信頼度に前季差分を表示 | Sonnetにより再実装：`goToNextTurn()`の**冒頭**（他の処理が動く前）で`gs.trustPrev=Object.assign({},gs.trust)`を保存するようラップし、結果画面側で`t-prevT`の差分（+n/-n）を状態文言の前に追加表示 | 実装済み（要確認） |
| 15 | 2026-07-15 | 仕様変更 | 輸送ETAのノード表記を`NODE_LOC`表記に変更（スコープ拡大：皇子バー・輸送依頼サマリも対象） | Sonnetにより実装：`etaToNodeText`・出発Toast・皇子バー`pc-sub`/`ltab-dl`表示・輸送依頼サマリ（`ensureOrderUI`内と旧経路の両方）の計7箇所で`NODE_LOC[node]`を使うよう修正。**重大な副作用バグを発見・修正済み**：`var NODE_LOC={...}`は実は別のIIFE内に閉じ込められておりグローバル未公開だった。皇子がノード1以上（前線）にいる状態で`updatePrinceBar`等から参照すると`ReferenceError: NODE_LOC is not defined`が発生し、これが「T3に進まない」バグの真因だった（`goToNextTurn`チェーンの途中で例外→setPhase('letter')まで到達しない）。`window.NODE_LOC=NODE_LOC;`を1行追加して解消。jsdomで実機相当のシミュレーション（前線に皇子がいる状態で6ターン連続送り）を行い、エラー0件・毎回`letter`フェーズへ正常遷移することを自動テストで確認済み | 完了（自動テスト済み・要実機最終確認） |
| 16 | 2026-07-15 | 仕様変更 | 市場価格を`seasonPriceDelta`のみにする | **原因判明・修正済み**：計算ロジック自体は元から`seasonPriceDelta`のみを参照していたが、HTML内に埋め込まれた`MARKET_ECON`のデータが`data/market_economy.json`への手動編集（wood/food_matのdelta値）と食い違っていた（同期漏れ）。`node data/repair_market_embed.js`を実行して再同期し、埋め込みデータとjsonの一致を確認済み | 完了 |
| 17 | 2026-07-15 | バグ（重要度高） | スマホで手紙フェーズを開くと、皇子タブ（.tab-bar-5）がタイトルバー（#hdr）付近で隠れる／潰れて見える（スクリーンショット: IMG_8558.jpg。「[新着：]」表記が重なって読めず、左端のタブが画面外に切れている） | mou_isso_v0_6_1.html：`#hdr`（L10付近、`position`指定なし＝通常フロー）、`#overlay`（L120付近、`position:fixed;inset:0;z-index:50`）、`.tab-bar-5`/`.tab-btn-5`（L124-136付近、`flex:1;min-width:0`で5個並ぶが、`.tab-pname`等のフォントサイズ9px前後・折返し設計が狭い画面幅で崩れる可能性）。`@media(max-width:500px)`のブレークポイント（L251付近）はアイコンサイズ調整のみでタブバー自体の対応なし。原因は①`#overlay`が`#hdr`を覆わずその下に続けて表示される構造自体の問題か、②`.tab-bar-5`内要素が狭い画面で折り返し・重なりを起こしているレイアウト崩れか、実機での要素インスペクトが必要。**【修正プラン】** `#hdr`(z-index:100)は`#overlay`(z-index:50)より前面のため理論上は隠れないはずで、実態は②（狭い画面幅で5タブが収まらずoverflow/折返しで欠ける）の可能性が高い。①`.tab-bar-5`に`overflow-x:auto;-webkit-overflow-scrolling:touch`を追加し、収まらない場合は横スクロールで全タブへアクセス可能にする。②`#overlay`内の先頭コンテンツに`#hdr`実測高さ分のpadding-topを確保し、重なりを物理的に排除。③実機（iPhone Safari等）またはDevToolsモバイルエミュレータで`#hdr`高さとタブ位置を再現・実測してから最終値を確定すること（要実機確認）。**実機確認結果（ユーザー）**：T2以降は改善したが、T1のみ依然としてタブが隠れる／崩れる。T1固有の状態（未読の新着表示・DOM構築タイミング等）に起因する残存原因を追加調査要。**【Codex追加調査】** 原因：T1ではWebフォント読込等により`#hdr`の高さが初期化後に変化するが、従来は初期化時とDOMContentLoaded時にしか高さを測定していなかった（T2以降は高さが安定済みのため再現しない）。修正：L11132付近、`ResizeObserver`で`#hdr`の高さ変化を継続監視し、Webフォント読込完了時（`document.fonts.ready`）にも再測定するよう変更。確信度：中（実機確認要） | 修正済み（要実機再確認：T1） |
| 18 | 2026-07-15 | バグ（重要度高） | スマホで手紙フェーズの内容を上から下に勢いよくスクロールすると、ページがリロードされてしまう | mou_isso_v0_6_1.html：`body`（L9付近）に`overflow`制限がなく、`#overlay`（L120付近）は`overflow-y:auto`だが`overscroll-behavior`が未設定。ページ全体がネイティブスクロールのままのため、`#overlay`内スクロールの端で行き止まった勢いがブラウザのpull-to-refreshに伝播しリロードされていると推測。`body`または`#overlay`に`overscroll-behavior:contain`（可能なら`overscroll-behavior-y:none`）を追加する対応が有力候補。**【修正プラン】** `body`(L9)に`overscroll-behavior-y:contain;`を追加し、加えて`#overlay`(L120)にも`overscroll-behavior:contain;`を追加。CSS追加のみで既存ロジックへの影響なし、低リスク。**実機確認結果（ユーザー）**：OK、リロードされなくなった | 完了 |
| 19 | 2026-07-15 | バグ | スマホの画面幅が広め（500px超）の場面で、経営フェーズ等の皇子バー（依頼内容表示）が右にずれる／崩れる。「準備：」等の後で改行させたい | mou_isso_v0_6_1.html：`.pc-req`（L25、`#prince-bar`内の依頼状況表示。`white-space:nowrap;overflow:hidden;text-overflow:ellipsis`で強制1行・省略表示になっており、内容が長いと`.pc`行全体が右へ押し出される可能性）。テキスト生成元は`req.textContent='準備：'+...`のパターンが複数箇所にある（L2168, L2785, L3466, L4713付近）。**採用方針：②生成側で「準備：」の直後に改行（`<br>`）を入れる**（①の`white-space:nowrap`解除だけだとラベルと内容の途中で不格好に割れる可能性があるため）。該当4箇所（L2168, L2785, L3466, L4713付近）の`req.textContent='準備：'+...`を`req.innerHTML='準備：<br>'+...`に変更する。`#prince-bar{grid-template-columns:repeat(4,1fr)}`（L19）の列幅・`.pc-info{min-width:0;flex:1}`（L23）との兼ね合いも確認要。**【修正プラン】** 実際の該当4箇所は`req.textContent='準備：'+...`のL2137, L2754, L3435, L4724（および`req.textContent='準備：'+lab`のL4724類似箇所）。すべて`req.innerHTML='準備：<br><span style="white-space:normal">'+内容+'</span>'`に変更（`q.label`/`q.item`等はゲーム内部データのみでHTMLエスケープ不要・XSSリスクなし）。`<br>`後の内容行のみ`white-space:normal`にして折返しを許容し、`.pc-req`本体の`white-space:nowrap`（L25）はラベル行に残す。書き換え後は500px超の実機/エミュレータで折返し崩れがないか確認要。**実機確認結果（ユーザー）**：改行はできている。ただしあと2文字分（例：「南)」まで）が見えてほしい、との軽微な追加要望あり。重要度低・ついでの対応でOK。**【Codex追加調査】** 原因：4分割グリッドから紋章幅等を引いた`.pc-info`の表示幅が狭く、内容部分がわずかに収まっていなかった。修正：L26付近、内容用span（`.pc-req span`）のフォントサイズを9px→8.5pxに縮小（「準備：」ラベル自体のサイズは維持）。確信度：中（端末のフォントレンダリング次第で見える文字数が変わるため実機確認要） | 修正済み（要実機再確認） |
| 20 | 2026-07-15 | 仕様変更 | 市場等でボタンを連打（ダブルタップ）すると、ブラウザのダブルタップズームと誤認識され画面がズームして崩れる。制御してほしい | mou_isso_v0_6_1.html：`<meta name="viewport" content="width=device-width,initial-scale=1">`（L3、`maximum-scale`/`user-scalable`指定なし）、ボタン類（`.qty-btn`, `.qty-btn-lg`等）に`touch-action`指定なし。対応案：①ボタン類に`touch-action:manipulation`をCSSで指定（ダブルタップズームのみ防止・ピンチズームは温存、推奨）、②viewportに`maximum-scale=1,user-scalable=no`を追加（手軽だがピンチズーム自体を封じるためアクセシビリティ面で要検討）。まず①を試すのが望ましい。**【修正プラン】** ①を採用。個別クラスへの追加漏れを防ぐため、`button, [onclick], .qty-btn, .qty-btn-lg, .tab-btn-5 { touch-action: manipulation; }`のようにまとめて指定する（既存の`.qty-btn`L192・`.qty-btn-lg`L228・`.tab-btn-5`L125等を含む共通ルールとして追加）。viewportのmaximum-scale等は見送り。**実機確認結果（ユーザー）**：OK | 完了 |
| 21 | 2026-07-15 | バグ（重要度高） | スマホでOK付きの確認ポップアップが現れない。具体的には①未返信の手紙があるのに手紙フェーズ完了確認がスキップされてそのまま経営フェーズへ進んでしまう、②皇子の出立確認ができない（出立操作自体が完了しない） | mou_isso_v0_6_1.html：どちらもブラウザ標準の`window.confirm()`を使用している——①`tryGoToMgmt()`内`if(!confirm('返信していない手紙があります...'))return;`（同名関数が複数箇所で再定義：L2703, L6116, L7341, L7761付近。最後に定義されるL7761付近が実行時に有効）、②`sortie(key)`内`if(!confirm(S('sortie_confirm',...)))return;`（L3413付近。同名関数もL1976, L2713, L3406付近に複数定義あり、L3406付近が実行時に有効と思われる）。**原因未特定**：`window.confirm()`は本来モバイルでも動作するはずだが、以下の可能性が考えられる：(a) LINE等のアプリ内蔵ブラウザ（in-app webview）で開いている場合、`confirm()`が抑制/自動応答されることがある、(b) #20のダブルタップズーム誤爆でボタンのタップ自体が正しく発火していない、(c) 何らかのJSエラーで`confirm()`に到達する前に処理が止まっている。実機のブラウザ種別（Safari/Chrome/アプリ内蔵ブラウザ）の特定が必要。**恒久対策の方向性**：`window.confirm()`依存をやめ、既存の`save-modal`等と同じ自前のOKボタン付きモーダル（コード内実装済みパターンを流用）に置き換えるのが、環境非依存で確実。**【修正プラン】** 現行コード再調査の結果、対象`confirm()`は実質2種：a)手紙未返信確認「返信していない手紙があります。このまま経営フェーズへ進みますか？」＝L7377・L7798・L10303の3箇所（`window.tryGoToMgmt=function(){...}`がL7370とL7790で二重に再代入されており、スクリプト順で**最後に代入されたL7790版**が実行時に有効。L10303は別関数内のため要関数名特定）、b)出立確認`confirm(S('sortie_confirm',...))`＝L3382（`sortie=function(key){...}`がL3375開始で、これも**ファイル順で最後の代入**が有効）。対応：既存`.save-modal`（L96-99, L1318-）と同じ見た目を流用し、`showConfirmModal(message, onOk)`のような汎用コールバック型モーダル関数を新設。各`if(!confirm(...))return;`パターンを`showConfirmModal(msg, function(){ /*confirm以降の元処理をそのまま移植*/ });`に置換する。**注意**：confirm以降の後続処理をコールバック内へ**漏れなく**移植しないと「OK押下後に処理が実行されない」新規バグを生むため、置換後は各呼び出し箇所でOK/キャンセル双方の分岐を実機で確認すること。L2298, L2312, L2486, L4881, L6481, L6577（セーブ/馬関連）のconfirmは今回の対象外なので変更しない。**実機確認結果（ユーザー）**：OK | 完了 |
| 22 | 2026-07-15 | バグ | 人足（輸送手段）が使用後もいつまでも返ってこない。例：T3に2名出発、輸送に2ターンかかりT5到着、しかしT6時点でも空き人足が2/4のまま（本来は帰還して4/4に戻るはず）。**期待動作の補足（ユーザー確認）**：T5到着の翌ターンであるT6輸送時点で4/4に戻っているのが正しい挙動 | mou_isso_v0_6_1.html：未調査（人足の空き数管理・往復ロジック。`portersFree()`関連および出発時の消費／到着後の返却処理を要特定） | 未着手 |
| 23 | 2026-07-15 | バグ | 兵糧太がT5でN1の依頼を完了したのに、結果フェーズにその納品実績が表示されない（T6結果フェーズに1ターン遅れて表示される）。**期待動作の補足（ユーザー確認）**：手紙はT6の手紙ターンに届いており、達成自体はT5に起きているはずなので、この実績はT5の結果フェーズで表示されてほしい | mou_isso_v0_6_1.html：未調査（#11の生産在庫スナップショット・#12の手渡し済み記録と同系統の「その季にやった実績を結果画面に出す」処理群を要確認。#24-26と共通で「1ターン遅れて表示される」症状のため、ターン送り内の実績確定タイミングとスナップショット取得順序を横断的に確認要）。**【Codex調査・修正】** 原因：輸送到着を確定する`tickConvoys()`が結果画面表示前ではなく、次ターン（T6）へ進む操作中に実行されていたため、当ターンの結果に反映されていなかった。修正：`goToNextTurn`内の旧`tickConvoys()`呼び出し（L3588付近）を削除し、`goToResult()`直前（L11185付近）で`tickConvoys()`→`refreshFacilityUnlockCaps()`→`positionAllPieces()`の順に確定処理を行うよう変更。#24-26と共通の原因・共通の修正。確信度：高（T5進行シミュレーションで確認済み） | 修正済み（要実機再確認） |
| 24 | 2026-07-15 | バグ | 荷車の解禁が今季の出来事ログに赤字で表示されない（T6結果フェーズに1ターン遅れて表示される）。**期待動作の補足（ユーザー確認）**：荷車はT6時点で実際に使用可能になっているため、解禁イベントはT5の結果フェーズで表示されてほしい | mou_isso_v0_6_1.html：未調査（#9で実装した`pushTurnEvent`/`gs.turnEvents`のうち、荷車/船/馬車解禁＝赤のイベント発火箇所が荷車の解禁処理から呼ばれていない可能性。#23・#25・#26と同一の1ターン遅延パターンのため共通原因を疑う）。**【Codex調査・修正】** #23と同一原因（`tickConvoys()`のタイミング遅延）。#23の修正で同時に解消。確信度：高（要実機再確認） | 修正済み（要実機再確認） |
| 25 | 2026-07-15 | バグ | 「市場を強化」の進言が選択可能になったことが今季の出来事ログに赤字で表示されない（T6結果フェーズに1ターン遅れて表示される）。**期待動作の補足（ユーザー確認）**：市場はT6時点で実際に解放されているため、解禁イベントはT5の結果フェーズで表示されてほしい | mou_isso_v0_6_1.html：未調査（#9の進言解放＝赤のイベント発火箇所が市場強化の解放条件成立時に呼ばれていない可能性。#8の調査内容とも関連するため合わせて確認。#23・#24・#26と同一の1ターン遅延パターンのため共通原因を疑う）。**【Codex調査・修正】** 原因：N1クリアの遅延（#23と同一原因）に加え、市場強化の解放上限を更新する`refreshFacilityUnlockCaps()`も結果描画前に呼ばれていなかった。修正：L11188付近、到着・クリア確定後、結果描画前に施設解放条件を再計算するよう変更（#23の修正と同じ箇所）。確信度：高（要実機再確認） | 修正済み（要実機再確認） |
| 26 | 2026-07-15 | バグ | 皇子がN2に到着した扱いになっているのに、地図上のコマはN1のマスに表示されたまま（結果フェーズ・ノード移動表示がT6に1ターン遅れる）。**期待動作の補足（ユーザー確認）**：ノード移動自体はT5に起きているはずなので、コマの表示もT5の結果フェーズ時点でN2に更新されてほしい | mou_isso_v0_6_1.html：未調査（#15で修正した`NODE_LOC`表記やノード管理ロジックとコマの描画位置更新処理の間にズレがある可能性。地図上のコマ位置を更新する箇所を要特定。#23-25と同一の1ターン遅延パターンのため共通原因を疑う）。**【Codex調査・修正】** 原因：`gs.node`のN2更新と`positionAllPieces()`がT6開始時まで発生していなかった（#23と同一の遅延構造）。修正：L11189付近、T5結果描画前にコマ位置を更新するよう変更（#23の修正と同じ箇所）。確信度：高（要実機再確認） | 修正済み（要実機再確認） |
| 27 | 2026-07-15 | バグ | 手紙フェーズで、信頼度（信頼度20以上）と進捗度（ノード到着）の手紙条件が同じターンに発生すると、進捗の手紙に返信したつもりが信頼度の手紙に差し替わり、「信頼度の手紙に返信した」ことになってしまう（進捗の手紙への返信が失われる）。**あるべき仕様（ユーザー確認）**：同ターンに競合した場合、親密度（信頼度）側の手紙は次のターンに繰り越して発生するのが正しい | mou_isso_v0_6_1.html：未調査（同ターン内で複数の手紙候補（進捗系・信頼度系）が同時成立した際の選択・上書きロジックを要特定。信頼度手紙を次ターン繰り越しにする仕様変更で対応する方針） | 未着手 |
| 28 | 2026-07-15 | 仕様変更 | 弩（どの）が解禁されたのはよいが、市場画面で弩だけがアイコン（絵）を表示している。市場では弩の絵を出さなくてよい | mou_isso_v0_6_1.html：未調査（市場の品目アイコン描画箇所で弩のみ個別にアイコン画像を出している可能性。他品目との表示統一のため弩のアイコン表示を除去） | 未着手 |
| 29 | 2026-07-15 | バグ | 結果フェーズでOK付きの解禁メッセージ（モーダル）が出てくる。#10で機能していない解禁モーダル（`showPendingUnlockModal`）を無効化・削除したはずだが、別の同種メッセージが表示されている模様。対象を要調査 | mou_isso_v0_6_1.html：未調査（#10で無効化した`showPendingUnlockModal`とは別に、結果フェーズ側でOK付きモーダルを出している呼び出し元を特定する必要あり） | 未着手 |
| 30 | 2026-07-15 | バグ | 王（皇帝？）の進言で解禁されたはずの「市場を強化」「工房に人を増やす」が選択できない | mou_isso_v0_6_1.html：未調査（進言解放条件・`kgPick`内の`market_up`/`staff_smith`の解禁チェック（`marketLv/marketLvMax`, `smithStaff/smithStaffMax`）と、王の進言による解放処理との連携を要特定。#25・#8とも関連する可能性あり） | 未着手 |
| 31 | 2026-07-15 | バグ | 「このターンの出来事」ログに「弩★3・在庫3」のような表示が出るようになった。不要なので削除してほしい。あわせて、他に解放されたもの（#28の弩アイコン等と同様に）についても、同じ出来事ログに余計な表示が出ていないか確認してほしい | mou_isso_v0_6_1.html：未調査（#9の`pushTurnEvent`/`gs.turnEvents`関連処理のうち、弩解禁または在庫関連の処理から意図しないイベントテキストが積まれている可能性。#28（市場アイコン）と合わせて弩関連の表示周りを横断的に確認要） | 未着手 |

## 状態の凡例

- `未着手` … 登録のみ
- `プラン済` … 修正プラン作成・承認待ち／承認済み
- `修正中` … GPT にて実行中
- `完了` … 修正・動作確認済み
