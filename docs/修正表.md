# 修正表（バグ・仕様変更の受付リスト）

> 運用ルール：
> 1. バグ・仕様変更はまずこの表に**貯めるだけ**（すぐ修正しない）
> 2. 「修正プランを立ててください」の指示で Claude がプランを作成
> 3. プラン承認後、実行修正は ChatGPT (GPT-5.6 sol) に依頼

| # | 日付 | 種別 | 内容 | 該当箇所（分かれば） | 状態 |
|---|------|------|------|----------------------|------|
| 1 | 2026-07-15 | バグ | 輸送時に在庫があるのに「現庫0」と表示されることがある（例：剣3→表示0、＋押下で表示2に減る） | mou_isso_v0_6_1.html：現庫表示の更新経路（changePrepQty L1933 / updateTransportUI L2154 / ensureOrderUI L9479、stockOf 二重定義 L3051・L6228） | 完了 |
| 2 | 2026-07-15 | バグ | T2以降、市場の前期比が全品目±0になる。**原因特定済み**：ターン送り時にコアgoToNextTurn内のupdateInvDisplay→refreshMarketUIが先に新季節価格へ再計算した後、L5779のrecomputePrices(true)がprevBuyを新価格で上書きするため。最小修正案＝L5779をrecomputePrices(false)に（prevBuy保存はL8481/L6892の事前スナップショットに任せる）。データ（seasonPriceDelta）は正常でHTML埋め込みとも一致確認済み | mou_isso_v0_6_1.html：L5779（recomputePrices(true)）／L2028（コアのupdateInvDisplay）／L5805・L8481・L6892のラッパー群 | 完了 |
| 3 | 2026-07-15 | 仕様変更 | 進言費用を「信頼度（褒める）＝300両」以外はすべて150両に | mou_isso_v0_6_1.html：adviceCost()定義、kgPickラッパーL6409、費用表示L6404・L10806 | 完了 |
| 4 | 2026-07-15 | バグ | ノード2にいる武器の皇子・兵器の皇子へ輸送するとき、品目行（アイコン・現庫・依頼残・輸送中の行）の表示が小さく・暗くなる（スクリーンショットあり: スクリーンショット 2026-07-14 204111.png。剣の行が灰色がかって文字が薄い） | 原因特定：新レンダラの `.tp-item-line` は暗い輸送パネル上で背景なし・12px、補足文字は暗色 `--faded` のため、ロック行のように小さく暗く見えていた。動的な locked クラス付与やアイコン注入親styleはなし。品目行限定で明るい panel 背景・通常文字色・opacity:1・13pxを指定 | 完了 |
| 5 | 2026-07-15 | バグ | 馬の王子が出立後、同じターン内にすぐ納品（馬の選択画面）ができてしまう。仕様：出立を押したターンは何もできず、納品は次ターン以降にすること | `goTransportHorsePrince()` の既存ガードを確認し、`buildHorsePrinceSendUI()` にも sortieTurn/turn ガードを追加。同ターンは馬チェック欄を案内文へ置換し、既存Goボタンも除去 | 完了 |
| 6 | 2026-07-15 | 仕様変更 | 輸送手段「荷車」の表記と仕様：現在「荷車(空き50/50・積載2)」→ 正しくは「荷車(要20金・積載2)」。使用時に金20両かかるが積載2で運べる仕様にする。また荷車の解禁時に通知がなかったので、結果フェーズ等でOKボタン付きポップアップで「お金（20両）がかかりますが容量2で運べます」の内容を告知する | 荷車ラベルを「要20金・積載2」に統一。最終の貨物・台数検証後かつ在庫消費前に20両不足ガードと課金を追加。既存 `pendingUnlocks` のOK付きモーダルを利用し、解禁本文に費用と積載量を明記 | 完了 |
| 7 | 2026-07-15 | バグ | #3修正の残件：「王子を褒める」の進言も150両で課金されてしまう（正しくは300両）。その他の進言が150になったのは正しい | `kgPick` 全再代入を確認。課金する2箇所を `window.adviceCostFor(val)` 優先へ統一し、最終ラッパーがクリック時の `val` を保持することも確認。`commend` は300、その他は150を直接判定し、保持値は `finally` で確実にクリア | 完了 |

## 状態の凡例

- `未着手` … 登録のみ
- `プラン済` … 修正プラン作成・承認待ち／承認済み
- `修正中` … GPT にて実行中
- `完了` … 修正・動作確認済み
